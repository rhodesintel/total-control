# TotalControl Flutter App

## Quick Start
Read `/home/priv/claudes/totalcontrol/HANDOFF.md` for latest session context.

## Think Chain
Use `mcp__think__think_get` with branch `totalcontrol` for continuity.

## Build & Run
```bash
flutter build linux --release
./build/linux/x64/release/bundle/total_control
```

## Key Files
- `lib/models/rule.dart` - Rule model with AND conditions
- `lib/screens/home_screen.dart` - Sentence-style UI
- `lib/main.dart` - App entry, theme colors

## Extension
Browser extension at `/home/priv/claudes/totalcontrol/extension/`
